/**
 * Guards Export Barrier — platform-admin
 * 
 * GATE 4.1 — EXPORT BARRIER
 * Controlled exports for guards.
 */

export { DenyAllGuard } from './deny-all.guard';
export { ExplicitAllowGuard } from './explicit-allow.guard';
