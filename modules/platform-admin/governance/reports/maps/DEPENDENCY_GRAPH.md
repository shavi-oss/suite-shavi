# DEPENDENCY GRAPH — Platform Admin

(modules/platform-admin)

## Import Relationship Summary

### Module Level (Entry Point)

- **PlatformAdminModule** (`modules/platform-admin/platform-admin.module.ts`)
  - **Imports**:
    - `PrismaModule` (`src/db/prisma.module.ts`)
  - **Controllers**:
    - `HealthController` (`controllers/health.controller.ts`)
    - `InternalUserController` (`src/internal-users/internal-user.controller.ts`)
    - `OrgMappingController` (`src/org-mapping/org-mapping.controller.ts`)
  - **Providers**:
    - `DenyAllGuard` (`guards/deny-all.guard.ts`) — **APP_GUARD**
    - `OrganizationRepository` (`src/organizations/organization.repository.ts`)
    - `InternalUserService` (`src/internal-users/internal-user.service.ts`)
    - `InternalUserRepository` (`src/internal-users/internal-user.repository.ts`)
    - `OrgMappingService` (`src/org-mapping/org-mapping.service.ts`)
    - `OrgMappingRepository` (`src/org-mapping/org-mapping.repository.ts`)
    - `CoreClient` (`src/core-adapter/core.client.ts`)
    - `AuditService` (`src/audit/audit.service.ts`)
    - `AuditRepository` (`src/audit/audit.repository.ts`)

### Feature Level (Inferred Flow)

- **Audit**:
  - `AuditService` -> `AuditRepository` -> `PrismaService` (via `AuditRepository`)
- **Core Adapter**:
  - `CoreClient` -> `fetch` (Core API `GET /api/v1/organizations/:id`)
  - Evidence: `CORE_CONTRACT_V1_EXTRACT.md`
- **Internal Users**:
  - `InternalUserController` -> `InternalUserService` -> `InternalUserRepository` -> `PrismaService`
- **Org Mapping**:
  - `OrgMappingController` -> `OrgMappingService` -> `OrgMappingRepository` & `CoreClient` -> `PrismaService`

### Packaging Boundary

- **Source**: `modules/platform-admin/src/` (Logic), `modules/platform-admin/controllers/` (Root API), `modules/platform-admin/guards/` (Security).
- **Dist**: `dist/` (Generated by `tsc`, not present in source tree).
- **Runtime Entrypoint**: NONE. This is a library module consumed by the Host App via `import { PlatformAdminModule } from 'suite-shavi'`.
- **Packaging Artifact**: `npm pack` generates `.tgz` from `dist/` content (mapped via `package.json` files list).

## Note on Inference

This graph is derived from actual file paths and direct inspection of `platform-admin.module.ts`, `core.client.ts`, and `audit.service.ts`. Deeper internal dependencies are inferred from standard NestJS architectural patterns.
