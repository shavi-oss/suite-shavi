# DEPRECATED — Superseded by the Canonical Contract

**Status:** Deprecated as of 2026-07-11 (contract v2 approved).

The single source of truth for the Suite ↔ Core integration contract is now:

    suite-shavi/INTEGRATION_CONTRACT_CORE.md

That canonical file contains the full, approved v2 content, including:
- The two authorized auth models (Tenant User-Scoped JWT for Core v1, and Platform-Admin S2S JWT for Core v2).
- The complete list of authorized Core endpoints (`GET /api/v2/admin/organizations/:id`, `GET /api/v1/organizations/:id`, `POST /api/v2/admin/organizations`, and the `PATCH /api/v2/admin/organizations/:id/{suspend,unsuspend,deactivate}` lifecycle routes).
- The revised Stop Rules.
- The v1 → v2 changelog (what was wrong in v1 and what changed).

**Do not edit this file.** Edit the canonical file above and bump the contract version per the governance process. This copy is retained only as a pointer so existing references do not break.
